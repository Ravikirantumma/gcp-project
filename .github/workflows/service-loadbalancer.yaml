apiVersion: v1
kind: Service
metadata:
  name: demo-service-loadbalancer
  namespace: default
  labels:
    app: demo-app
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    app: demo-app
